{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d && bun run src/index.ts",
    "db:push": "docker compose up -d && bun --bun run drizzle-kit push",
    "better-auth:generate": "bunx --bun @better-auth/cli generate --output=./src/db/schema/better-auth.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^1.0.0-beta.1-fd8bfcc",
    "postgres": "^3.4.7",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "drizzle-orm": "^1.0.0-beta.1-fd8bfcc",
    "nanoid": "^5.1.6"
  }
}
